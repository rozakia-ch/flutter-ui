import 'package:flutter/material.dart';

class MoneyBankingScreen extends StatelessWidget {
  const MoneyBankingScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Money Banking"),
    );
  }
}
